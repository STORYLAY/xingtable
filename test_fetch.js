import fetch from 'node-fetch';

async function run() {
  const token = 'test'; // Probably doesn't need auth if we just hit something or I might need the exact headers.
  // Wait, I can just create a React component that fetches and renders it. I already did.
}
run();
